# 2026-09-06 请求记录

## 用户请求
按照 `docs/instructions.md` 指引，依据 `docs/技术方案.md`，完成 `docs/prd/大家一起跳.v1.md`（多人跳一跳·棋盘版·实时竞速）的微信小游戏开发。

## 澄清确认（先思考再编码）
- 范围：搭建三端骨架 + 核心玩法闭环（房间→选图→开局→蓄力跳跃→结算）
- 客户端：原生微信小游戏（JS + Canvas 2D）
- MVP 后置占位：分享卡片/好友排行榜(开放数据域)/断线重连/防作弊（以最简形式先落地）
- 规则裁定：PRD 4.2/4.3 歧义 -> 采用“直线段安全步数 d + 蓄力上限 N=min(剩余,6)”模型，s>d 飞出回起点

## 本次产出
- 服务端 we_jump_dotnet/WeJump.Api（.NET8 minimal API + 原生 WebSocket + Dapper/MySQL + Redis 令牌会话 + 微信 code2session(留空可本地模拟) + 房间内存态 + 回合制对局引擎 GameEngine(权威结算/出界/模式A冠军倒计时10s/模式B超时/落库)）
- 客户端 we_jump_wx（game.js 入口 + app.js 场景编排 + scenes(main/room/game/result) + 蓄力/上划取消交互 + 棋盘渲染 + 断线自动重连重入房间 + openDataContext 占位 + recordResult 云存储上报钩子）
- SQL：docs/sql/20260906-we-jump-init.sql 与 docs/sql/init-ddl.sql（user/game_record/game_player_result）
- README.md（协议表、运行方式、规则裁定说明、MVP/后置清单）

## 验证方式（遵守“不 build/不启站”）
- 客户端 JS 全部 node --check 语法通过
- 服务端 C# get_errors 无错误（Roslyn 设计时编译已产出 obj）

## 追加：编写项目 .gitignore
- 在仓库根创建 `.gitignore`：忽略 .NET 产物(bin/obj/.vs)、微信开发者工具个人配置(project.private.config.json)、node_modules、编辑器/系统文件、本地密钥覆盖配置
- 注意：`appsettings.json` 作为示例模板保留入库，仅忽略 appsettings.Local.json 等本地覆盖
