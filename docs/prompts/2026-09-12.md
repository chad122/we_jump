# 2026-09-12 请求记录

## 用户请求
修复服务端编译错误（`dotnet run` 报错）：
- Room.cs(56,38) CS0053：属性类型 IReadOnlyList<RoomPlayer> 可访问性低于 Room.Players
- Room.cs(61,24)/(66,24)/(71,23) CS0050：返回类型 RoomPlayer 可访问性低于 FindBySeat/FindByUser/AddPlayer

## 处理
- 根因：`RoomPlayer` 被声明为 `internal`，而公开类 `Room` 的公开成员暴露了它。
- 修复：将 `RoomPlayer` 改为 `public sealed class`（`we_jump_dotnet/WeJump.Api/Services/Room.cs`）。
- 主动排查并修复另一处潜在的同类编译问题：`Program.cs` 中 `/ws` 端点原写成 `app.Map("/ws", async (HttpContext ctx, GameHub hub) => ...)`，`IApplicationBuilder.Map` 仅接受单参数 `RequestDelegate`，改为在委托内 `ctx.RequestServices.GetRequiredService<GameHub>()` 获取依赖。

## 结果
- `get_errors` 对 we_jump_dotnet 报告无错误。

## 追加：修复 GameEngine 引用缺失成员
- 报错：GameEngine.cs(341/349/383) CS1061 RoomPlayer 未包含 FinalRank 定义。
- 原因：写 RoomPlayer 时遗漏 FinalRank 字段（结算名次时会用到）。
- 修复：Room.cs 为 RoomPlayer 增加 `public int FinalRank = 0;`；GameEngine.Reset() 中一并清零，避免“再来一局”残留上局名次。

## 追加：小游戏提示“微信登录凭证校验失败”
- 根因：客户端 `project.config.json` 的 appid 是 `touristappid`（游客），而服务端配置了真实 AppID `wxe3e5573dd7efbd7c`；code 与 appid 不匹配，code2session 返回 40029 invalid code，服务端返回 401。
- 修复：
  1. `we_jump_wx/project.config.json` 的 appid 改为与服务端一致的真实 AppID；
  2. `WxAuthClient` 改为返回结构化 `WxSessionResult(OpenId, Error)`，透传微信 errcode/errmsg；`AuthService` 改为返回 `LoginOutcome`；`Program` 登录接口把真实原因带给客户端；
  3. README 增补登录排错与 AppSecret 安全提示（建议迁到 appsettings.Local.json / 环境变量）。

## 追加：用户决定
- 用户回复“不需要”，即暂不拆分 appsettings.json / appsettings.Local.json，保持当前配置结构不变（真实 AppSecret 仍留在 appsettings.json）。

## 追加：开发者工具报 “worker path empty”
- 现象：`[] worker path empty  Error: worker path empty (... env: macOS,mg; lib: 3.17.3)`，来自微信开发者工具内部（mg=小游戏），非业务代码报错。
- 处理：
  1. 加固 `we_jump_wx/project.config.json`：补齐官方小游戏字段（description、isGameTourist:false、debugOptions、scripts），`libVersion` 由 `2.30.0` 改为官方支持的 `latest`（避免指定版本解析不到导致基础库 worker 路径为空），并加 `ignoreDevUnusedFiles:false` 防止工具误过滤手写模块；
  2. README 新增「开发者工具报 worker path empty」排查步骤（导入目录必须是 we_jump_wx 且类型为小游戏、清缓存、compileType=game、切换调试基础库、工具版本/重启）。

## 追加：按用户要求清理并准备重新导入
- 用户确认：导入目录=we_jump_wx、项目类型=小游戏；要求直接删掉重新导入。
- 排查：项目内无 project.private.config.json；开发者工具 CLI 因「服务端口未开启」无法直接打开项目（需在工具 设置→安全设置 开启服务端口）。
- 处理：
  1. 将 `we_jump_wx/openDataContext/` 占位目录移出游戏根目录到 `docs/openDataContext-placeholder/`（开放数据域在工具侧是独立 worker 子上下文，未声明的占位目录是潜在干扰源；接入时再放回并配置）；
  2. `game.json` 收敛为官方最小形态（仅 deviceOrientation）；
  3. 游戏根目录现仅剩 game.js / game.json / project.config.json / js/，结构最干净；
  4. README 目录结构与说明同步更新。

## 追加：小游戏请求报 HTTP 403
- 现象：`WAGame.js:1 POST http://127.0.0.1:5000/api/auth/login 403 (Forbidden)`。
- 排查结论：服务端不产生 403（curl 实测 `/healthz`=200、`/api/auth/login`=401；带 Origin 的 POST 仍 401；OPTIONS 预检=405）。WebSocket 握手=101。故 403 来自客户端/工具侧链路（工具网络层或代理），非服务端代码问题。
- 结果：用户自行处理后回复“不用了，能用了”，本次未做代码改动。


